# --- Compiled Java output ---
build/
bin/
out/
*.class

# --- OS generated files ---
.DS_Store

# --- Local DB config (each dev keeps their own; copy from config.properties.example) ---
config.properties